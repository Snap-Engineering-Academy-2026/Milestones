# Curl?

Open Terminal. _I like to do this with: Command+Space then typing "Terminal"_

Try running these commands. What do they do?

```bash
curl google.com
```
```bash
curl example.com
```
```bash
curl http://numbersapi.com/random/trivia
```
```bash
curl http://api.open-notify.org/astros.json
```
```bash
curl https://letscountapi.com/milken/SEA
```
```bash
curl -X POST https://letscountapi.com/milken/SEA/increment
```
```bash
curl -X POST https://letscountapi.com/milken/SEA/decrement
```

### Questions:

 - What do you think `curl` does? 
 - Do they do the same thing when you run them multiple times?
 - What is the url/link thing after `curl`? 
 - Why do the last two have `-X POST` added?

</br>

</br>

</br>

</br>
